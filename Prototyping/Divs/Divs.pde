/*
*/
//
println(displayWidth, displayHeight);
fullscreen();
int appWidth = displayWidth;
int appHeight = displayHeight;
//
int paperWidth = 120;
int paperHeight = 280;
float DivX = appWidth * ??? / paperWidth;
float DivY = appHeight * ??? / paperHeight;
float DivWidth = appWidth * ??? / paperWidth;
float DivHeight = appHeight * ??? / paperHeight;
//
//DIVs

//Settings Button
float DivX = appWidth * 110 / paperWidth;
float DivY = appHeight * 0 / paperHeight;
float DivWidth = appWidth * 10 / paperWidth;
float DivHeight = appHeight * 20 / paperHeight;

//Record Player
float DivX = appWidth * 20 / paperWidth;
float DivY = appHeight * 10 / paperHeight;
float DivWidth = appWidth * 80 / paperWidth;
float DivHeight = appHeight * 60 / paperHeight;

//Module One
float DivX = appWidth * 22 / paperWidth;
float DivY = appHeight * 18 / paperHeight;
float DivWidth = appWidth * 10 / paperWidth;
float DivHeight = appHeight * 10 / paperHeight;

//Module Two
float DivX = appWidth * 22 / paperWidth;
float DivY = appHeight * 30 / paperHeight;
float DivWidth = appWidth * 10 / paperWidth;
float DivHeight = appHeight * 10 / paperHeight;

//Module Three
float DivX = appWidth * 22 / paperWidth;
float DivY = appHeight * 42 / paperHeight;
float DivWidth = appWidth * 10 / paperWidth;
float DivHeight = appHeight * 10 / paperHeight;

//Module Four
float DivX = appWidth * 22 / paperWidth;
float DivY = appHeight * 54 / paperHeight;
float DivWidth = appWidth * 10 / paperWidth;
float DivHeight = appHeight * 10 / paperHeight;

//Record
float DivX = appWidth * 50 / paperWidth;
float DivY = appHeight * 20 / paperHeight;
float DivWidth = appWidth * 40 / paperWidth;
float DivHeight = appHeight * 40 / paperHeight;

//Picture on Record
float DivX = appWidth * 60 / paperWidth;
float DivY = appHeight * 30 / paperHeight;
float DivWidth = appWidth * 20 / paperWidth;
float DivHeight = appHeight * 20 / paperHeight;

//Slider
float DivX = appWidth * 20 / paperWidth;
float DivY = appHeight * 75 / paperHeight;
float DivWidth = appWidth * 80 / paperWidth;
float DivHeight = appHeight * 5 / paperHeight;

//Loop Button
float DivX = appWidth * 25 / paperWidth;
float DivY = appHeight * 85 / paperHeight;
float DivWidth = appWidth * 10 / paperWidth;
float DivHeight = appHeight * 10 / paperHeight;

//Rewind Button
float DivX = appWidth * 35 / paperWidth;
float DivY = appHeight * 85 / paperHeight;
float DivWidth = appWidth * 10 / paperWidth;
float DivHeight = appHeight * 10 / paperHeight;

//Back Button
float DivX = appWidth * 45 / paperWidth;
float DivY = appHeight * 85 / paperHeight;
float DivWidth = appWidth * 10 / paperWidth;
float DivHeight = appHeight * 10 / paperHeight;

//Pause/Play
float DivX = appWidth * 55 / paperWidth;
float DivY = appHeight * 85 / paperHeight;
float DivWidth = appWidth * 10 / paperWidth;
float DivHeight = appHeight * 10 / paperHeight;

//Next Button
float DivX = appWidth * 65 / paperWidth;
float DivY = appHeight * 85 / paperHeight;
float DivWidth = appWidth * 10 / paperWidth;
float DivHeight = appHeight * 10 / paperHeight;

//Skip Forward Button
float DivX = appWidth * 75 / paperWidth;
float DivY = appHeight * 85 / paperHeight;
float DivWidth = appWidth * 10 / paperWidth;
float DivHeight = appHeight * 10 / paperHeight;

//Shuffle Button
float DivX = appWidth * 85 / paperWidth;
float DivY = appHeight * 85 / paperHeight;
float DivWidth = appWidth * 10 / paperWidth;
float DivHeight = appHeight * 10 / paperHeight;

//Seperation
size(400, 400);
line(0, 100, 120, 100);

//Song One Pic
float DivX = appWidth * 10 / paperWidth;
float DivY = appHeight * 110 / paperHeight;
float DivWidth = appWidth * 20 / paperWidth;
float DivHeight = appHeight * 20 / paperHeight;

//Song Two Pic
float DivX = appWidth * 10 / paperWidth;
float DivY = appHeight * 140 / paperHeight;
float DivWidth = appWidth * 20 / paperWidth;
float DivHeight = appHeight * 20 / paperHeight;

//Song Three Pic
float DivX = appWidth * 10 / paperWidth;
float DivY = appHeight * 170 / paperHeight;
float DivWidth = appWidth * 20 / paperWidth;
float DivHeight = appHeight * 20 / paperHeight;

//Song Four Pic
float DivX = appWidth * 10 / paperWidth;
float DivY = appHeight * 200 / paperHeight;
float DivWidth = appWidth * 20 / paperWidth;
float DivHeight = appHeight * 20 / paperHeight;

rect( DivX, DivY, DivWidth, DivHeight );
rect( DivX, DivY, DivWidth, DivHeight );
rect( DivX, DivY, DivWidth, DivHeight );
rect( DivX, DivY, DivWidth, DivHeight );
rect( DivX, DivY, DivWidth, DivHeight );
rect( DivX, DivY, DivWidth, DivHeight );
rect( DivX, DivY, DivWidth, DivHeight );
